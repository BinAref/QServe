<?xml version="1.0" encoding="utf-8"?>
<!-- The glyph, wrapped so its level drives its angle. A rotate drawable turns
     from 0 to 360 degrees across levels 0..10000, which is what the dropdown
     animates when its menu opens. -->
<rotate xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_chevron_glyph"
    android:fromDegrees="0"
    android:toDegrees="360"
    android:pivotX="50%"
    android:pivotY="50%" />
